[build]
builder = "NIXPACKS"
buildCommand = "poetry install"

[nixpacks.pkgs]
pkgs = ["build-essential", "python3-dev"]

[deploy]
startCommand = "poetry run python src/app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
