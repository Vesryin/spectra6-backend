[build]
builder = "NIXPACKS"
buildCommand = "pip install --upgrade pip && poetry install"

[nixpacks.pkgs]
pkgs = ["build-essential", "python3-dev"]

[deploy]
startCommand = "poetry run python src/app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
